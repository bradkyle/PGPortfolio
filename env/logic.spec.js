
describe('bitfiex wrapper funcitonality', () => {

    describe("ordering process", () => {

        // - Recieves incoming vector: [['BTC', 0.1], ['ETH', 0.4], ['TRX', 0.5]] etc.
        // - Derives current portfolio weight vector: [[BTC: 0.4], ['ETH', 0.3], ['TRX', 0.3]] etc.
        // - Get delta PV in portfolio weight vector: [['BTC', 0.3], ['ETH', -0.1], ['TRX': -0.2 ]]
        // -

        it("Should sort current portfolio vector", async () => {

        });

        it("Should make only one request to place order via sorting", async () => {

        });

        it("Should make only one request to place order", async () => {

        });
    });

});
